# 현황

**마지막 갱신: 2026-08-18**

## 지금 상태 한 줄

**코드 미결은 전부 닫혔다.** 예전 `🔴 미결` 표의 1~9번은 모두 해결됐는데 표에 그대로 남아 있어서
아래 §닫힌 미결로 내렸다.

**사용자가 실제로 할 일은 A 하나다.** B는 같이 하고, C는 안 해도 아무 일 없고, D는 자동으로 돈다.

## 🔴 열려 있는 것

| # | 할 일 | 누가 | 지금 |
|---|---|---|---|
| A | **엑셀 업로드를 실제 파일로 한 번 돌려본다** | 사용자 | 코드는 수정·회귀 18건 통과 (§6번). 실물 확인만 남음 |
| B | **`app_probe` 2단계 → 결론 내고 워크플로 제거** | 같이 | 1단계 결과 나왔다 (§관찰). 다음은 Disable 후 2~3일 |
| C | 죽은 CSV 4개를 지울지 판단 | 사용자 | `data/snowball/monthly/{FAS,SOXL,TBT,TMF}_과거_데이터.csv`. 갱신은 멈췄고 아무도 안 읽는다. **급하지 않다** — 안 지워도 아무 일 없다 |
| D | `기록/DGbrain-이관대기.md` 위키 편입 | 자동 | **매일 02:07 클라우드 routine이 가져간다** (2026-08-18~) |

✅ **D의 절반은 궤도에 올랐다** (2026-08-18). `기록/` 폴더를 git에 넣었고(`fa37839`), "백테스트를
돌렸으면 묻지 말고 곧바로 이관대기에 적는다"가 `CLAUDE.md` §DGbrain 규칙이 됐다. **쌓는 건 자동이다.**

✅ **가져가는 쪽도 자동이 됐다** (2026-08-18). 원인은 클라우드 routine `DGbrain 이관`의
**소스 저장소에 `my_quantum_jump`가 없었던 것**이었다(DGbrain·makeMoney 둘뿐). 소스에 추가하고
주기를 **주 1회 → 매일 02:07 KST**(`7 17 * * *` UTC)로 바꿨다.
routine: `https://claude.ai/code/routines/trig_01EL6kSWGQvTXnwdoUbtgCVJ`

⚠️ **routine은 GitHub만 본다.** 이관대기에 적고 **push하지 않으면 그날 밤 회수되지 않는다.**
`기록/`을 git에 넣은 이유가 이것이다. 로컬에만 있으면 클라우드 에이전트에게는 없는 파일이다.

**대기 중: 또 메리츠 · 연금 2종 실사(220변형) · 또 ISA 확정 2건 — 총 3절** (맘·쏘 삼성 건은 편입 완료, 절 삭제만 남음).

⚠️ **연금 실사 결과는 8/18까지 회수 루트 밖에 있었다.** 원본이 `C:\Users\DGgram\DGbrain_인계_연금전략_실사_2026-08-18.md` —
레포 밖 홈 폴더라 routine이 볼 수 없었다. 이관대기로 요약해 옮겼다. **작업 결과를 레포 밖에 두면 자동 회수에서 샌다.**

> ℹ️ **시작금이 0원이라 `시작일 대비` 열이 `현재 수익`과 같은 값을 보인다.**
> 코드가 `시작일 대비 = 현재 수익 − 시작금`으로 계산하기 때문(`pages/5_...py:359`).
> 종목을 채울 때 `📊 종합 요약` → 시작금 3개를 실제 투입 원금으로 넣어야 이 열이 의미를 갖는다.

## 최근 작업

### 2026-08-18

| 무엇 | 왜 | 결과 |
|---|---|---|
| 또 ISA 방어 바스켓에 `411060`(ACE KRX금현물) 추가 (3종→4종) | 금레버리지(225130)가 위험만 더 지고 있었다 | §또 ISA 방어 |
| `app_probe.yml`이 errexit에 걸려 죽던 것 수정 | 웜인 정상 상황에서 워크플로가 실패로 끝났다 | §관찰 |
| 현황.md 재정리 — 닫힌 미결을 표에서 내리고 열린 것만 남김 | 해결된 1~9번이 `🔴 미결`에 그대로 있었다 | 이 문서 |
| push 판단 기준을 **"매매 신호가 바뀌나"** 하나로 바꿈 | 문서 한 줄과 전략 상수를 똑같이 물어봐 헷갈렸다 | `CLAUDE.md` §push 판단 |
| `check.py` 추가 — 신호 지문 12개 대조 + 전 페이지 실행 | "신호 안 바뀌었다"를 눈으로 훑지 않고 증명한다 | 아래 §check.py |
| 이관대기 회수를 매일 02:07 자동으로 | routine 소스에 이 레포가 빠져 있었다 | 열린 항목 D |

⚠️ **전략 파라미터를 건드린 첫 변경이다.** 이 레포는 백테스트와 라이브가 같은 상수를 쓰므로
(`CLAUDE.md` §절대규칙 4) 이 한 줄이 곧 실제 매매 신호다. 근거와 한계를 §또 ISA 방어에 남겼다.

### 2026-08-14 (전부 라이브 반영 완료)

| 무엇 | 왜 | 결과 |
|---|---|---|
| 홈 USA500 요약 3~8위로 정정 | 전략은 3~8위인데 홈만 상위 10위 | PR #1 |
| 홈 KOSPI200 판정을 페이지와 일치 | 홈은 데일리+오늘, 페이지는 저번달 말 기준으로 각자 계산 | PR #2 |
| USA500 방어를 SPY 월봉 12개월선으로 통일 | 홈(월봉10)·페이지(일봉240)·백테스트(월봉12) 3방식 공존 | PR #3 |
| 스노우볼 로컬 우선 로딩 · 현재 달 기준 통일 · 죽은 코드 269줄 제거 | 콜드 부팅 낭비 + 홈/페이지 어긋날 여지 | PR #4 |
| 알파벳 종목코드 버그 수정 · 죽은 티커 3종 제거 | `00104K` → `000104`로 **조용히 다른 종목** | PR #5 |

**그날 반복해서 나온 원인은 하나다 — 같은 로직을 홈과 페이지에 따로 두면 반드시 갈라진다.**
판정·종목코드 규칙을 전부 공통 함수로 모았다. §지켜야 할 규칙 참조.

## check.py — push 전 검증 (2026-08-18 추가)

```
python check.py        # 약 2분
```

**같은 데이터 위에서 '지금 코드'와 '기준 커밋(HEAD) 코드'를 나란히 돌려** 6개 전략의
신호·백테스트 지문 12개를 대조하고, `app.py`와 켜져 있는 페이지를 `AppTest`로 실제 실행해
예외 0건을 확인한다. 기준 커밋은 git worktree로 따로 펼치므로 **작업 파일을 건드리지 않는다.**

⚠️ 저장해둔 기준값과 대조하지 **않는다.** `data/`가 매일 갱신돼 코드를 안 고쳐도 지문이
달라지기 때문이다. 그래서 매번 기준 커밋을 다시 돌린다 — 느린 대신 거짓 경보가 없다.

**언제 돌리나** — `CLAUDE.md` §push 판단의 🟡(앱 코드를 고쳤지만 신호는 그대로일 때).
통과해야 확인 없이 push한다. 🔴(전략 상수)에서 이게 변화를 잡아내는 건 정상이고,
그때 무엇이 어떻게 바뀌는지 보여주고 확인받는다.

## 지켜야 할 규칙

새 코드를 쓸 때 이 셋을 깨지 않는다. 셋 다 실제로 버그가 나서 생긴 규칙이다.

### 1. 판정 단일 출처 (2026-08-14 정리)

**투자/중지 판정을 두 곳에서 각자 계산하지 않는다.** 홈 대시보드와 상세 페이지가
따로 계산하다 어긋난 버그가 KOSPI·USA500 양쪽에서 나왔다. 지금은 함수 하나로 모았다.

| 전략 | 판정 함수 | 홈 | 페이지 |
|---|---|---|---|
| KOSPI200 | `utils/calculator.get_korea_market_status(df_month)` | `_kospi_status` | `pages/1` 탭1 |
| USA500 | `utils/us_helpers.get_usa_market_status(df_month)` | `_usa_status` | `pages/4` 탭1 |

- 입력은 **한 투자월치 월간 데이터**다. 저번달 말 선정일에 정해져 **한 달 내내 고정**이며 매일 바뀌지 않는다
- **데일리 탭2("오늘의 시장 상태")는 별개다.** 오늘 기준 참고용이라 일부러 합치지 않았다
- USA500 개월선은 `us_helpers.SPY_TIMING_MA_MONTHS`(=12) 한 곳에서 바꾼다. 백테스트 기본값과 같아야 한다
- ⚠️ 새 전략을 홈에 추가할 때도 **판정 로직을 홈에 복붙하지 않는다.** 공통 함수를 만들어 양쪽이 부른다

### 2. 종목코드 정규화는 한 곳뿐 (2026-08-14 정리)

`utils/data_loader.normalize_stock_code()` **하나만 쓴다.** `app.py`·`pages/5`는 이것을 import한다.
**파일마다 사본을 뒀기 때문에 같은 버그가 두 번 났다** (§5번 → §6번). 사본을 만들지 않는다.

### 3. 스노우볼 CSV는 로컬 우선 (2026-08-14 변경)

**컨테이너 디스크에 이미 있는 CSV를 먼저 읽는다.** raw URL은 폴백이다.

예전에는 순서가 반대였다. Streamlit Cloud 컨테이너에는 레포가 통째로 복사돼 있는데도
같은 파일 52개(미국 24 + 국내 28)를 GitHub raw로 매번 다시 받았다.

| | 스노우볼 로딩 | 홈 콜드 전체 |
|---|---|---|
| 예전 (raw 우선) | 7.98초 | 17.5초 |
| 지금 (로컬 우선) | **2.73초** | **11.5초** |

**로컬을 믿는 근거** — 앱의 나머지(`archive_kospi`·`archive_usa`·데일리 CSV)는 이미
디스크에서만 읽는다. 로컬이 낡는 상황이면 그쪽이 먼저 틀리므로, 스노우볼만 raw로 받아도
의미가 없다. 게다가 데일리 로봇이 매일 push → 그때마다 앱이 다시 뜨므로 체크아웃이 매일
새로 내려온다. 월 1회 갱신되는 스노우볼 CSV는 늦어도 하루면 반영된다.

⚠️ 파일이 없거나 못 읽으면 예전처럼 raw로 폴백한다. 최악이 '예전과 동일'이라 새 위험이 없다.
폴백은 로컬 디렉터리를 없는 경로로 돌려 실제로 동작함을 확인했다.
구현은 로더 5곳을 각각 고치지 않고 fetch 헬퍼 한 곳(`_load_csv`)에 모았다.

## 관찰 — 앱이 언제 잠드나 (`app_probe.yml`) · 열린 항목 B

9번을 판단할 재료를 모으는 중이다. **레포에 아무것도 커밋하지 않는다** — 결과는
Actions 로그에만 남는다. (커밋하면 그게 재배포를 유발해 관찰 대상이 오염된다)

**보는 곳**: GitHub → Actions 탭 → `App Probe (콜드 부팅 관찰)` → 각 실행의 Summary
**판정**: TTFB 8초 이상 = 콜드(잠들어 있었음) · 3초 미만 = 웜. 실측은 콜드 15~18초 / 웜 1~2초다.

### 1단계 결과 (2026-08-13 ~ 08-18, 58회) — **전부 웜**

**58회가 전부 "실패"로 끝났는데, 그 실패 자체가 데이터다.**
errexit 버그(오늘 수정)는 `awk "BEGIN{exit !($TTFB >= 8)}" && STATE=...` 줄에서 터지는데,
**awk가 0이 아닌 값을 낼 때만** 즉 `TTFB < 8초`(= 웜)일 때만 터진다.
콜드였다면 awk가 0을 반환해 대입이 성공하고 워크플로도 성공했을 것이다.

- 표본을 5건 뽑아 확인 — 전부 **종료 코드 1**. curl 자체가 실패했다면 `$TTFB`가 비어
  awk 문법 오류(종료 코드 2)가 났을 텐데 그런 실행이 없다
- 즉 **5일간 2시간 간격 58회 전부 웜.** 아래 표의 "일주일 내내 웜" 갈래에 해당한다

| 관찰 결과 | 결론 |
|---|---|
| 콜드가 한 번이라도 잡힘 | 2시간 간격 HTTP로는 잠드는 걸 못 막는다 → **ping 방식은 무효.** 9번 종료 |
| 일주일 내내 웜 | ✅ **여기 해당.** 잠들지 않고 있다. 다만 원인이 이 프로브인지 로봇 push인지 아직 모른다 → 2단계 |

⚠️ **TTFB 숫자는 한 줄도 안 남았다.** 요약을 쓰기 전에 죽었기 때문이다. 위 결론은 수치가
아니라 종료 코드로 낸 것이다.

**수정 후 첫 실행(2026-08-18 13:48 KST)이 성공했고 수치가 나왔다** — TTFB **1.270737초** ·
총 1.307020초 · HTTP 200 · 판정 `웜`. 예상 웜 구간(1~2초)에 들어와 위 추론을 뒷받침한다.

⚠️ **이제부터는 성공/실패로 웜·콜드를 구분할 수 없다.** 버그가 고쳐져 콜드여도 성공으로 끝난다.
판정은 각 실행의 **Summary에 적힌 TTFB**를 봐야 한다 (8초 이상 = 콜드). 그게 원래 의도된 동작이다.

### 2단계 (다음에 할 일)

Actions 탭에서 이 워크플로를 **Disable**하고 2~3일 둔다. 그 뒤 `Run workflow`로 수동 실행해 한 번 찍는다.

| 결과 | 결론 |
|---|---|
| 콜드 | 프로브가 앱을 깨워두고 있었다 = **ping 방식이 통한다.** 도입 여부를 판단 |
| 웜 | 로봇 push가 앱을 계속 살린다 = **ping 불필요.** 9번 종료 |

⚠️ "ping이 통한다"로 결론이 나도, 자주 ping해서 무료 플랜 자원을 계속 붙잡아 두는 게
맞는지는 별개 판단이다. 관찰용 2시간 간격과 상시 유지용은 다른 이야기다.
⚠️ 확인이 끝나면 이 워크플로는 **지운다.** 관찰용이지 상시 기능이 아니다.

## 또 ISA 방어 — KRX금현물 추가 (2026-08-18)

`utils/snowball.py`의 `KO_DEFENSE`에 **`411060`(ACE KRX금현물)** 을 더해 방어 후보를
3종 → **4종**으로 늘렸다. `225130`(금레버리지)은 **빼지 않았다.** 교체가 아니라 후보 추가다.

| | 전 | 후 |
|---|---|---|
| 방어 후보 | 217770 · 225130 · 455030 | 217770 · 225130 · 455030 · **411060** |
| 전기간 CAGR (2015-06~2026-07, 비용 0.25%) | 22.17% | **23.74%** |
| MDD | −18.6% | −18.6% |
| Sortino | 2.58 | **2.93** |
| 2022-01~ CAGR / MDD / Sortino | 33.25% / −17.8% / 2.95 | **37.46% / −13.4% / 3.62** |

**왜 바꿨나** — 겹치는 55개월(2021-12~)에서 두 금 상품의 CAGR은 23.0%로 **같은데**
MDD는 −45.9%(레버) vs −26.4%(현물), 월변동성은 1.65배 차이였다. 레버리지가 위험만 더 졌다.
방어자산 자리에 2배 레버리지·합성·환헤지 상품이 앉아 MDD −45.9%를 찍는 건 그 자체로 이상하다.
금현물은 환노출이라 원화 약세 국면(= 국내 위험회피 국면)에서 이중으로 방어된다.

**왜 교체가 아니라 추가인가** — 411060은 2021-12 상장이라 그 이전 구간은 방어 후보가
사실상 217770 하나만 남는다. 교체안의 전기간 성적(CAGR 20.93%, MDD −34.1%)은 성능 차이가
아니라 **데이터 공백**이다. 후보만 늘리고 선택은 `KO_DEF_WIN`(2M) 이격도 순위에 맡긴다.
후보를 없애는 쪽이 되돌리기 어려우므로 이번엔 하지 않았다.

⚠️ **표본이 얇다 — 이 변경은 통계적으로 유의하지 않다.** 2022-01 이후 방어 모드는 18개월,
보유가 실제로 달라진 달은 15개(9승 6패)다. 이득 대부분이 2022년 한 해(연수익 2.2% → 18.1%)에서
나오고 2023년은 오히려 손해(35.8% → 30.6%). 짝지은 t = 1.08. 방어 파라미터를 흔들면
6개 설정 중 3승 2무 1패. **채택 근거의 절반은 숫자가 아니라 위 구조적 판단이다.**

**되돌리려면** `KO_DEFENSE`에서 `'411060'`만 지운다. 다른 변경은 없다
(`pages/6`의 방어 후보 설명은 `len(KO_DEFENSE)`로 바꿔놔서 자동으로 따라온다).

**검증** — 라이브 엔진을 streamlit 없이 재현해 월수익 134개 전건 대조(최대 오차 0.0),
`app.py` + 페이지 4개를 `AppTest`로 실제 실행해 예외 0건. 적용 후 현재 신호(2026-07 기준)는
🛡️방어 · **ACE 골드선물레버리지(합성H) · ACE KRX금현물** (달러SOFR 자리를 금현물이 밀어냈다).

✅ **계좌 제약은 없다 (2026-08-18 사용자 확인).** 예전에 여기 적어둔 미확인 가정 — 중개형 ISA의
레버리지 ETF 사전교육 요건 · 연금계좌(IRP/DC)의 레버리지·인버스 매수 제한 — 은 해소됐다.
**방어 후보 4종을 전부 실제로 살 수 있다.** 바스켓을 다시 볼 이유가 없어졌다.

**기각한 후보 — 공격에 `261220`(KODEX WTI원유선물H) 추가. 안 한다 (2026-08-18 확정).**
전기간 +1.79%p, 3분할 구간·파라미터 9종·비용 0~1% 전부 개선으로 검토한 것 중 숫자는 가장
일관적이었다. 그런데 실제 보유가 96 공격월 중 22개월뿐이고 2026-04 한 달이 +17.4%p /
2026-05가 −13.7%p로 꼬리가 사납다(t=0.62). 방어의 WTI인버스와 같은 원자재에 양방향 베팅이
커지는 것도 걸린다. **보류가 아니라 도입하지 않는 것으로 닫았다.** 다시 꺼내지 않는다.

⚠️ **검토 방법의 한계** — 같은 134개월 위에서 25가지 조합(제외 10 + 추가 15)을 돌려 고른
결과다. 그러면 우연히 좋은 것이 반드시 하나는 나온다. 위 개선폭은 "이 표본에서 이만큼이었다"는
값이지 앞으로 실현될 값이 아니다.

## ✅ 닫힌 미결 — 무엇을 왜 고쳤나

번호는 예전 표 그대로 둔다 (`CLAUDE.md`가 §3번·§6번을 가리킨다).

| # | 무엇이었나 | 닫힌 날 |
|---|---|---|
| 1 | 구글 시트 공유 설정 확인 | 2026-08-12 · "제한됨" 확인 |
| 2 | 서비스 계정 개별 공유 후 "제한됨"으로 변경 | 2026-08-12 · 불필요, 이미 그 상태였다 |
| 3 | PRIVATE 전환 여부 결정 | 2026-08-12 · **PUBLIC 유지** (사용자) |
| 4 | 로컬 커밋 push | 2026-08-12 |
| 5 | 알파벳 종목코드가 섞이면 포트가 통째로 사라진다 | 2026-08-12 |
| 6 | 엑셀 업로드·리밸런싱이 알파벳 종목코드를 **조용히 다른 종목으로** 바꾼다 | 2026-08-14 |
| 7 | 속도 개선 1~3단계 (캐시) | 2026-08-13 |
| 8 | 속도 개선 4단계 — archive 확정분 합본(parquet) | 2026-08-13 |
| 9 | 속도 개선 5단계 — 콜드 부팅 | 2026-08-14 · 15~18초 → 11.5초. 잔여 판단은 열린 항목 B |
| 10 | 엑셀 업로드 실제 파일로 확인 | ⏳ **아직** — 열린 항목 A로 옮겼다 |

### 5번 — 종목코드 알파벳 버그

`00104K`(CJ4우 전환) 같은 **알파벳이 들어간 종목코드**를 `ddo`·`sso`·`mom`에 넣으면
그 포트폴리오가 화면에서 **통째로 사라졌다.** 에러 메시지도 안 떴다.

원인 — 두 읽기 경로가 종목코드를 다르게 처리했다.

| 경로 | 코드 | `00104K` |
|---|---|---|
| `StockInfo` 읽기 | `str(x).strip().zfill(6)` | ✅ `00104K` |
| 포트폴리오 읽기 | `str(int(float(str(x).strip()))).zfill(6)` | 💥 `ValueError` |

`float("00104K")`가 터지고, 이게 `except: pass`에 삼켜져 빈 DataFrame이 반환됐다.
해당 위치는 `pages/5_내 소형주 퀀트 포트.py:122`, `app.py:222` 두 곳.
`StockInfo` 마스터에 `00104K` `37550K` `37550L` 같은 코드가 이미 들어 있어 실제로 보유하게 될 수 있는 종목이다.

**수정 (2026-08-12).** 두 파일에 정규화 함수를 두고, 순수 숫자면 예전과 똑같이 `int(float())`을
태우고 변환이 실패하는 코드만 원본을 살렸다.

⚠️ **이때 파일마다 사본을 둔 것이 6번 버그의 원인이었다.** 지금은
`utils/data_loader.normalize_stock_code()` 한 곳뿐이다. §지켜야 할 규칙 2.

### 6번 — 엑셀 업로드가 알파벳 종목코드를 다른 종목으로 바꾼다

5번과 **원인이 달랐다.** 이쪽은 터지지 않고 **조용히 틀린 코드를 만들어** 더 위험했다.
엑셀 업로드와 리밸런싱 계산기가 `str.extract(r'(\d+)')`로 숫자만 뽑아 알파벳을 잘랐다.

| 입력 | 예전 | 지금 |
|---|---|---|
| `00104K` (CJ4우 전환) | `000104` 💥 다른 종목 | `00104K` ✅ |
| `37550K` (DL이앤씨우) | `037550` 💥 다른 종목 | `37550K` ✅ |
| `005930` · `5930` | `005930` | `005930` (동일) |
| `A005930` (증권사 접두) | `005930` | `005930` (동일) |

**수정 (2026-08-14).** 규칙을 `utils/data_loader.normalize_stock_code()` **한 곳**으로 모으고
세 경로(시트 읽기·엑셀 업로드·리밸런싱)가 이 함수를 쓴다. `app.py`·`pages/5`에 있던
사본 2개도 제거했다 — **사본을 뒀기 때문에 이 버그가 두 번 났다.**

접두 문자(`A005930`)는 계속 떼고, 알파벳 접미(`K`·`L`)는 살린다. 앞자리 0이 떨어진
`104K`도 `00104K`로 복원한다. 회귀 케이스 18건 전건 통과(기존 숫자 코드 동작 불변 포함).

### 7번 — 속도 개선 1~3단계

병목은 **캐시 밖에 있던 무거운 재계산**이었다. 셋 다 고쳤다.

| # | 무엇 | 어디 |
|---|---|---|
| 1 | archive 로드 + 전처리를 한 캐시로 묶음 (`load_us_master`) | `utils/us_helpers.py` · `pages/4` · `app.py` |
| 2 | 스노우볼 6개 전략의 신호·백테스트에 캐시 부착 (12곳) | `utils/snowball.py` |
| 3 | `load_archive_data`의 중복 `ttl="1h"` 제거 | `utils/data_loader.py` |

**측정 결과** (로컬, Streamlit `AppTest`로 페이지를 실제 실행. rerun 5회의 중앙값)

| 페이지 | 위젯 클릭 전 | 후 | 변화 |
|---|---|---|---|
| 4 USA500 | 1.61초 | **0.87초** | −46% |
| 6 스노우볼 | 2.97초 | **1.77초** | −40% |
| 6 스노우볼 (콜드) | 6.72초 | **4.00초** | −40% |
| 7 계절성 (손대지 않은 대조군) | 0.44초 | 0.41초 | 변화 없음 ✓ |

**동작이 바뀌지 않았음을 확인했다.** 스노우볼 6개 전략의 신호·백테스트 결과와
USA500 마스터 DataFrame, 총 13개의 내용 지문(sha256)을 변경 전후로 대조해 전건 일치.
5개 페이지를 `AppTest`로 실제 실행해 예외 0건.

⚠️ 왜 `preprocess_us_data`에 직접 데코레이터를 붙이지 않았나 — 그러면 Streamlit이
16만 행 DataFrame을 **인자로 해싱**해야 해서 이득을 깎아먹는다. 캐시 키는
`(폴더 경로, folder_hash)` 문자열 2개여야 한다. 비슷한 작업을 할 때 같은 함정을 조심한다.

### 8번 — archive 확정분 합본

**⚠️ 먼저 바로잡을 사실: `archive_*`는 월 1회만 바뀌지 않는다.**
데일리 로봇이 **최신월 파일 하나**의 `이번달수익률`을 매일 다시 쓴다
(`update_daily_us.py:127`, `update_daily_kr.py`의 `sync_archive_returns`).
지난 달들은 확정돼 다시 바뀌지 않는다.

그래서 합본에 **전부** 담으면 안 된다 — 매일 통째로 다시 커밋돼 레포가 폭증한다. 대신

```
합본(_history.parquet) = 확정된 지난 달 전부   ← 새 달이 시작될 때만 바뀐다
최신월 CSV 1개                                 ← 매일 바뀌지만 파일 1개라 싸다
```

| archive_usa | 읽기 |
|---|---|
| 예전: CSV 332개 | 1.58초 |
| 지금: 합본 + 최신월 CSV | **0.099초** (16배) |
| archive_kospi | 0.46초 → **0.014초** (32배) |

**형식을 parquet으로 고른 근거** — 단일 CSV와 실측 비교했다. parquet이 양쪽 다 이겼다.

| 형식 | 읽기 | 한 달 추가 시 .git 증가 |
|---|---|---|
| 단일 CSV | 0.30초 | 1,320 KB |
| parquet | 0.017초 | **28 KB** |

**안전장치** — 합본이 없거나·깨졌거나·지난 달 파일 목록과 어긋나면 조용히 예전처럼
CSV를 전부 읽는다. 최악의 경우가 '예전과 동일'이라 새로 생기는 위험이 없다.
유효성은 mtime이 아니라 **파일명 + 바이트 크기**로 판정한다 (mtime은 clone마다 바뀐다).

**검증** — CSV 전체 경로와 합본 경로의 결과를 행 순서·자료형·**전 셀 값**까지 대조해 일치.
manifest를 망가뜨렸을 때와 parquet을 망가뜨렸을 때 각각 폴백이 동작하고 결과가 같음도 확인.

⚠️ 합본은 로봇이 만든다 (`update_monthly_us.py`·`update_monthly_kr.py` 끝에서 호출).
손으로 만들지 않는다. 월간 워크플로 2개에 `pyarrow`를 추가했다 — 없으면 합본 생성이
실패하지만 `try/except`로 감싸 갱신 자체는 성공하고 앱은 CSV 폴백으로 돈다.
⚠️ 라이브가 읽는 `archive_usa`·`archive_kospi`만 합본을 만든다. 꺼진 페이지의
`archive_sp500`·`archive_korea`는 대상이 아니다 — 페이지를 켜면 그때 추가한다.

### 9번 — 콜드 부팅

**15~18초 → 11.5초.** 앞선 기록은 "앱이 잠들었다 깨는 시간이라 코드로는 못 고친다"고
단정했는데 **틀렸다.** 그 시간의 상당 부분은 슬립이 아니라 **불필요한 네트워크 왕복**이었다.

| 무엇 | 효과 |
|---|---|
| 스노우볼 CSV를 로컬 우선으로 (§지켜야 할 규칙 3) | 로딩 7.98 → 2.73초 |
| 죽은 티커 3종(FAS·SOXL·TMF) 로드 제외 | 24 → 21종 |
| → 홈 콜드 전체 | **17.5 → 11.5초** |

남은 11.5초 중 순수 '앱이 깨어나는' 시간은 코드로 못 줄인다. ping으로 깨워 두는
방법은 아래 두 가정이 미확인이라 아직 도입하지 않았다 → 열린 항목 B(§관찰).

1. **단순 HTTP 요청이 Streamlit의 '활동'으로 집계되는지 모른다.** curl이 받는 건
   HTML 껍데기뿐이고 실제 세션은 웹소켓으로 열린다. 웹소켓만 활동으로 친다면 무효다
2. **로봇이 push할 때마다 앱이 재시작될 가능성이 크다.** 그러면 깨어 있게 만들어도
   재시작으로 캐시가 날아가 이득이 줄어든다. ⚠️ 이건 §지켜야 할 규칙 3의 전제이기도 하다

### 3번 — PRIVATE 전환 판단 재료 (2026-08-12 확정)

**결론: 필수 아니다.** 보안 구멍은 시트 쪽에서 이미 닫혔다. 전략 로직을 감추고 싶은지가 유일한 쟁점이다.

PRIVATE으로 가서 **지켜지는 것**

- `utils/snowball.py`(95KB)·`utils/calculator.py`(42KB)의 전략 로직과 파라미터

PRIVATE으로 가도 **안 지켜지는 것 / 이미 안전한 것**

- 보유 종목 — 애초에 레포에 없다. 시트에 있고 시트는 "제한됨"이다
- 서비스 계정 키 — `st.secrets`에 있고 레포에 없다
- 시트 URL 노출 — 시트가 제한 공유라 URL을 알아도 못 연다
- `data/` CSV — 공개 시장 데이터라 감출 값어치가 없다

PRIVATE의 **비용**

- GitHub Actions가 Free 플랜 월 2,000분 한도를 받는다 (PUBLIC은 무제한). 실측 220분 = 11%. 여유 있다
- Streamlit 앱이 private 레포를 읽으려면 GitHub `repo` 권한 승인이 **먼저** 필요하다
- private 레포로 바꾸면 **앱도 자동으로 private이 된다.** 앱을 다시 public으로 되돌리는 건 앱 설정에서 가능하다
- Free 플랜은 private 앱을 **동시에 1개**만 허용한다. 앱이 1개뿐이라 걸리지 않는다

전환한다면 **이 순서를 지킨다** (거꾸로 하면 앱이 죽는다)

1. Streamlit → Settings → Linked accounts → Source control → GitHub **private repo 접근 승인**
2. GitHub → repo Settings → 맨 아래 Danger Zone → Change visibility → **Private**
3. Streamlit 앱 재부팅 → 뜨는지 확인
4. 앱을 다시 공개로 쓰려면 → 앱 설정 → Sharing → **Public**

## ✅ 2026-08-12 확인된 사실

| 항목 | 결과 |
|---|---|
| 시트 "일반 액세스" | **제한됨** — 링크 유출돼도 안 열린다. 보유 종목 노출 없음 |
| 시트 서비스 계정 권한 | **편집자** — 이게 맞다. 낮추면 안 된다 (아래 ⚠️) |
| 시트 탭 구성 | `ddo` `sso` `mom` `Config` `StockInfo` 5개 — 코드가 요구하는 것과 일치 |
| `Config` 탭 | **정상 작동 확인.** 앱 화면의 시작일이 기본값(오늘)이 아닌 `2026-06-01`로 떴다 = 시트에서 읽어온 값이다 |
| `StockInfo` 탭 | **사용 중.** 사용자가 액면가·상장주식수를 손으로 채우는 종목 마스터다. 헤더 `종목코드` `종목명` `시장구분` `액면가` `상장주식수`, G1·H1에 `기준일 2026-05-08` 메모. 종목코드는 앞자리 0이 잘려 있으나 코드가 `zfill(6)`으로 복원하므로 문제없다 |
| 포트폴리오 데이터 | **비어 있다.** `또`·`쏘`·`맘` 종목 0개, 시작금 3개 모두 0원. **사용자가 10월부터 쓸 예정** |
| 서비스 계정 키 | 레포에 없음. `st.secrets`로 분리돼 있다 |
| 계좌번호·개인식별정보 | 없음. 별칭(`맘·쏘 삼성`)뿐 |
| 시트 URL 하드코딩 | `app.py:211`, `pages/5_내 소형주 퀀트 포트.py:15` 2곳 |
| `.gitignore` | 없었음 → 추가함 |
| 꺼진 페이지 2개 | `2_KOREA_모멘텀`, `3_SP500_모멘텀` — 확장자를 뗀 **의도된 상태** (사용자 확인) |
| Actions 로봇 9개 | 최근 12회 확인 — 전부 success |
| 데이터 신선도 | 전 CSV 2026-08-11 종가까지. 정상 |

⚠️ **서비스 계정을 뷰어로 낮추지 않는다.** 코드가 시트에 **쓴다**
(`save_portfolio_to_gsheet` `save_config_to_gsheet` → `ws.clear()` + `ws.update()`).
뷰어로 바꾸면 앱의 저장 버튼이 `저장 오류`를 뱉는다.
*(이 문서의 이전 판에 "뷰어로 추가"라고 적혀 있었다 — 틀린 지시였다. 2026-08-12 정정)*

⚠️ **시트를 손으로 꾸미지 않는다.** 앱이 저장할 때 `ws.clear()`로 탭을 통째로 지우고 다시 쓴다.
`ddo`·`sso`·`mom`·`Config`에 메모 열이나 서식을 더해도 저장 한 번에 사라진다. A~D 4열만 남는다.
`StockInfo`는 코드가 읽기만 하므로 자유롭게 관리해도 된다.
**헤더는 손으로 맞출 필요가 없다** — `ddo`·`sso`·`mom`·`Config` 4개는 앱이 저장할 때 1행 헤더를
직접 쓴다. 손으로 만드는 건 읽기 전용인 `StockInfo`뿐이다.

## 이력

- **2026-08-12** — GitHub에서 로컬 클론(`orca/projects/my_quantum_jump`). 보안 실사, `.gitignore`·`CLAUDE.md` 추가. 클론 전에는 Streamlit 배포 전용이었고 클로드 코드로 작업한 적 없음
- **2026-08-12** — 시트 공유 설정 "제한됨" 확인. 보안 미결 해소. 서비스 계정 권한 지시 오류 정정(뷰어→편집자). PRIVATE 판단 재료 확정
- **2026-08-12** — 헤더 수동 확인 항목 철회(앱이 자동으로 쓴다). `Config` 정상 작동 확인. 포트폴리오는 아직 비어 있음
- **2026-08-12** — 사용자 확인: `StockInfo`는 **사용 중**이다(직전 판의 "미사용" 기록은 오류, 정정). PUBLIC 유지 결정. 포트폴리오는 10월부터 사용 예정. 종목코드 알파벳 버그를 발견해 5번으로 등록
- **2026-08-13** — 속도 실사. 병목은 캐시 밖 재계산이었다. 1~3단계 적용해 USA500·스노우볼 페이지의 위젯 반응을 약 40~46% 단축(7번). 결과 불변은 지문 13개 대조로 확인
- **2026-08-13** — 4단계 적용(8번). archive 읽기 16~32배 단축. **`archive_*`가 월 1회 갱신이라던 앞선 기록은 틀렸다** — 데일리 로봇이 최신월 파일을 매일 고친다. 그래서 합본을 '확정된 지난 달'로 한정했다. 5단계는 가정 두 개가 미확인이라 관찰 워크플로를 붙이고 판단 보류(9번)
- **2026-08-13** — 홈 대시보드 USA500 요약이 상위 10위를 보여주던 것을 전략과 동일한 3~8위로 정정. 상세 페이지(`pages/4`)는 원래 정상, `app.py`만 어긋나 있었다. `app.py`에 `USA_RANK_START/END` 상수를 두고 `pages/4`의 `STRAT_RANK_START/END`와 일치시킨다는 주석을 달았다 (PR #1, main `ff04597`)
- **2026-08-14** — 홈 KOSPI200 판정이 페이지 탭1과 어긋나던 것을 고쳤다. 홈은 데일리 파일 + `datetime.today()` 지수 MA로, 탭1은 저번달 말 선정일 기준으로 각자 판정해 갈렸다(홈 ✅ / 탭1 🛑). `utils/calculator.get_korea_market_status()`로 판정을 합쳤다 (PR #2, main `02cf6e1`)
- **2026-08-14** — USA500 방어 판정을 **SPY 월봉 12개월선**으로 통일했다. 홈(월봉 10) · 페이지 탭1(일봉 240) · 백테스트(월봉 12) **3가지 방식**이 공존했고, 2026-04월엔 홈=이탈·페이지=정상으로 실제로 갈렸다. `utils/us_helpers.get_usa_market_status()`로 합치고 `SPY_TIMING_MA_MONTHS=12` 상수를 뒀다. 야후(`^GSPC`) 의존이 빠져 네트워크 차단에도 안정적이다 (PR #3, main `9b337d6`)
- **2026-08-14** — 전 페이지 실사(AppTest 실행 + 홈↔페이지 값 대조). 예외 0건, KOSPI·USA500·스노우볼 전부 일치 확인. 여기서 나온 3건을 아래에 처리했다
- **2026-08-14** — 폐지된 '맘 삼성'의 죽은 코드 269줄 제거(`render_samsung` 등). `run_backtest_samsung`은 라이브 `run_backtest_so`가 재사용하므로 유지. 라이브 6개 전략 지문 전건 일치로 동작 불변 확인
- **2026-08-14** — 스노우볼 홈 카드가 '현재 달'을 페이지와 다른 기준으로 골랐다(홈=`defensive` 있는 달 / 페이지=보유 정해진 달). 워밍업 구간·신규 ETF 미생성 시 두 화면이 다른 달을 가리킬 수 있었다. 홈을 페이지 기준으로 통일
- **2026-08-14** — **스노우볼 CSV를 로컬 우선으로 읽게 바꿨다.** 컨테이너에 이미 있는 파일을 두고 raw URL로 52개를 다시 받고 있었다. 로딩 7.98→2.73초, 홈 콜드 17.5→11.5초
- **2026-08-14** — 🔴 **6번 해결.** 엑셀 업로드·리밸런싱이 알파벳 종목코드를 조용히 다른 종목으로 바꾸던 버그를 고쳤다. 규칙을 `utils/data_loader.normalize_stock_code()` 한 곳으로 모으고 사본 2개(`app.py`·`pages/5`)를 없앴다. 회귀 18건 통과
- **2026-08-14** — 폐지된 맘 삼성이 쓰던 `FAS`·`SOXL`·`TMF`를 앱 로드 대상에서(24→21종), `TBT`까지 로봇 수집 대상에서 뺐다. 아무 전략도 안 읽는 파일을 매일 만들고 매번 받고 있었다. 기존 CSV는 지우지 않고 갱신만 멈춘다 (→ 열린 항목 C)
- **2026-08-18** — 또 ISA 방어 바스켓에 `411060`(ACE KRX금현물) 추가(3종→4종). 금레버리지(225130)는 남겼다 — 411060 상장 이전 구간이 비어 교체는 비교 자체가 불가능하다. 전기간 CAGR 22.17%→23.74%, MDD 유지, Sortino 2.58→2.93. **유의성은 없다**(t=1.08, 방어 표본 18개월) — 근거의 절반은 구조적 판단(레버리지가 같은 수익에 1.65배 위험)이다. 공격에 원유(261220) 추가는 검토했으나 꼬리가 사나워 보류
- **2026-08-18** — `app_probe.yml`이 `awk ... && VAR=x` 때문에 errexit에 걸려 죽던 것 수정. 웜(정상)일 때 awk가 1을 반환해 워크플로가 실패로 끝났다. 콜드 판정 임계값 주석도 15초→8초로 실제 로직에 맞췄다(동작 변경 없음)
- **2026-08-18** — **1단계 관찰 결과를 읽어냈다.** 58회 실패 기록 자체가 "전부 웜"이라는 데이터였다(§관찰). 2단계로 넘어갈 조건 충족
- **2026-08-18** — 현황.md 재정리. 해결된 1~9번이 `🔴 미결` 표에 그대로 남아 있어 열린 것과 닫힌 것을 분리했다. 흩어져 있던 원칙 3가지를 §지켜야 할 규칙으로 모으고, 커밋 안 된 `기록/DGbrain-이관대기.md`를 열린 항목 D로 등록
- **2026-08-18** — **push 규칙을 바꿨다.** 기준이 `commit이냐 push냐`에서 **"이 변경이 매매 신호를 바꾸나"** 하나로 옮겨졌다(사용자 확정). 문서 한 줄과 전략 파라미터의 위험이 전혀 다른데 예전 규칙은 똑같이 물어봐 헷갈렸다. 문서·로봇은 알아서, 신호 불변 코드는 `check.py` 통과 시 알아서, 전략 상수와 되돌리기 어려운 것만 확인받는다. `CLAUDE.md` §push 판단
- **2026-08-18** — `check.py` 추가. 지문 12개 + 페이지 실행으로 '신호 불변'을 증명한다. 음성 검증도 했다 — `KO_DEFENSE`에서 411060을 일부러 빼니 또 ISA 지문 2개만 정확히 잡고 나머지 10개는 통과
- **2026-08-18** — 이관대기 회수가 안 돌던 원인을 찾았다. 클라우드 routine `DGbrain 이관`의 **소스 저장소에 `my_quantum_jump`가 없었다**(DGbrain·makeMoney 둘뿐). 추가하고 주 1회 → **매일 02:07 KST**로 바꿨다. ⚠️ routine은 GitHub만 보므로 **push하지 않으면 회수되지 않는다**
- **2026-08-18** — `CLAUDE.md` §구조에 `check.py`·`기록/`을 넣고, 워크플로 개수를 '9개'에서 '로봇 9 + 관찰용 1'로 정정
