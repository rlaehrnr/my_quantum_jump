# 현황

**마지막 갱신: 2026-08-12**

## 🔴 미결 — 순서대로

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **구글 시트 공유 설정 확인** | 미확인 |
| 2 | 시트가 공개면 → 서비스 계정 개별 공유 후 "제한됨"으로 변경 | 1번 결과 대기 |
| 3 | **PRIVATE 전환 여부 결정** | 1번 결과 대기 |
| 4 | **로컬 커밋 1개 push 여부 결정** | 대기 |

### 1번 하는 법

시트 URL은 `app.py:211`. 시트 열기 → 우측 상단 **[공유]** → 하단 **"일반 액세스"**

- **제한됨** → ✅ 안전. 2번 건너뛴다
- **링크가 있는 모든 사용자** → 🔴 보유 종목 노출. 2번으로

### 2번 순서 (거꾸로 하면 앱이 죽는다)

1. Streamlit → 앱 → App settings → **Secrets** → `google_credentials` 안의 `client_email` 복사
   (`...@....iam.gserviceaccount.com`)
2. 시트 [공유]에 그 이메일을 **뷰어**로 추가
3. 그다음 "일반 액세스" → **제한됨**
4. 앱 새로고침 → 소형주 화면 뜨는지 확인

### 3번 판단 재료

PRIVATE으로 가면 GitHub Actions가 Free 플랜 **월 2,000분 한도**를 받는다 (PUBLIC은 무제한).
실측 사용량 **월 약 220분 = 11%**. 비용은 문제 없다.
Streamlit이 private 레포를 읽으려면 **먼저** Streamlit → Settings → Linked accounts에서
GitHub 접근을 승인해야 한다. 승인 없이 PRIVATE으로 바꾸면 앱이 죽는다.

### 4번 대기 중인 커밋

`0193d27 chore: .gitignore · CLAUDE.md 추가` — 로컬에만 있음 (`main` ahead 1).
push하면 라이브 재배포가 돌지만, 문서 2개라 앱 동작에는 영향 없다.

## ✅ 2026-08-12 확인된 사실

| 항목 | 결과 |
|---|---|
| 서비스 계정 키 | 레포에 없음. `st.secrets`로 분리돼 있다 |
| 계좌번호·개인식별정보 | 없음. 별칭(`맘·쏘 삼성`)뿐 |
| 시트 URL 하드코딩 | `app.py:211`, `pages/5_내 소형주 퀀트 포트.py:15` 2곳 |
| `.gitignore` | 없었음 → 추가함 |
| 꺼진 페이지 2개 | `2_KOREA_모멘텀`, `3_SP500_모멘텀` — `.py` 확장자가 없어서 Streamlit이 안 읽는다. 고장이 아니라 **꺼둔 상태** |
| Actions 로봇 9개 | 전부 정상. 최근 20회 모두 success |

## 이력

- **2026-08-12** — GitHub에서 로컬 클론(`orca/projects/my_quantum_jump`). 보안 실사, `.gitignore`·`CLAUDE.md` 추가. 클론 전에는 Streamlit 배포 전용이었고 클로드 코드로 작업한 적 없음
