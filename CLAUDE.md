# my_quantum_jump — 퀀트 대시보드

**라이브 서비스다.** Streamlit Cloud에 배포돼 실제로 돌아가는 앱이고, 여기 데이터로 실제 자산을 운용한다.
작업할 때 이 문서를 먼저 읽고 그대로 따른다.

- 앱: `myquantumjump-bccuvb2zu4yacfnqd9exjc.streamlit.app`
- 레포: `rlaehrnr/my_quantum_jump` — **PUBLIC**. 2026-08-12 검토 후 유지 결정 (사유는 `현황.md` §3)
- 배포 브랜치: `main`. **main에 push하면 즉시 재배포된다**

## 세션 시작 시

1. **`현황.md`를 먼저 읽는다.** 미결 항목이 거기 있다
2. `git status`로 push 안 된 커밋이 있는지 본다
3. 미결 항목이 있으면 사용자가 묻기 전에 먼저 짚는다

## 절대 규칙

1. **push 전에 반드시 확인받는다.** push = 라이브 사이트 즉시 반영. 로컬 커밋까지는 자유
2. **`.streamlit/secrets.toml`을 커밋하지 않는다.** 구글 서비스 계정 키다. `.gitignore`에 있지만 `git add -f`로 뚫지 않는다
3. **`data/` 를 손으로 고치지 않는다.** GitHub Actions 로봇 9개가 갱신하는 실데이터다. 손으로 고치면 다음 갱신에 덮이거나 충돌한다
4. **백테스트와 라이브를 분리하지 않는다.** 코드에 *"라이브 = 백테스트 기본값, 한 곳에서 관리"* 라고 명시돼 있다. 이게 이 레포의 설계 의도다 — 갈라놓으면 검증한 파라미터와 실제 파라미터가 어긋난다
5. **결론은 여기 안 쓴다.** DGbrain으로 간다 (§DGbrain)
6. **한국어**
7. **보안 금지 항목을 기록하지 않는다** (§보안)

## 구조

```
app.py                   메인 화면 (소형주 현황 요약 포함)
pages/                   전략별 화면. Streamlit이 .py만 페이지로 읽는다
  1_KOSPI200_모멘텀.py
  2_KOREA_모멘텀          ← 확장자 없음 = 꺼져 있음
  3_SP500_모멘텀          ← 확장자 없음 = 꺼져 있음
  4_USA500_모멘텀.py
  5_내 소형주 퀀트 포트.py
  6_스노우볼 포트.py
  7_계절성_사이클.py
utils/                   calculator · data_loader · snowball · ui_components · us_helpers
update_*.py         9개  데이터 갱신 스크립트. Actions가 호출한다
.github/workflows/  9개  자동 갱신 로봇
data/          CSV 1028  시장 데이터. 로봇 소유
archive_*/               과거 버전 보관
```

**페이지를 끄려면 `.py`를 떼고, 켜려면 붙인다.** 파일을 지우지 않는다.

## 자동 갱신 로봇 (`.github/workflows/`)

| 주기 | 로봇 | 실측 소요 |
|---|---|---|
| 매일 | US Daily · KR Daily · Snowball | 90 / 72 / 41초 |
| 평일 | KR Gold · VIX | 38 / 28초 |
| 월말~월초 | KR Monthly · US Monthly · Seasonality · Snowball KR | 각 1~2분 |

월 합계 약 **220분** (2026-08-12 실측, 분 단위 반올림 기준).
PUBLIC 레포는 Actions가 무제한 무료다. **PRIVATE으로 바꾸면 Free 플랜 월 2,000분 한도가 적용된다** — 220분이면 11%라 여유는 있다.

⚠️ PRIVATE 전환은 **순서를 틀리면 앱이 죽는다.** Streamlit의 GitHub private repo 접근 승인이 먼저다. 절차는 `현황.md` §3번 판단 재료.

## 로컬에서 실행하려면

```
pip install -r requirements.txt
streamlit run app.py
```

구글 시트를 읽는 화면(`app.py` 소형주 요약, `pages/5_...`)은 `.streamlit/secrets.toml`에
`google_credentials`가 있어야 뜬다. 없으면 그 부분만 실패하고 나머지는 돌아간다.

## 보안

PUBLIC 레포다. 커밋 히스토리는 지워지지 않는다.

| 기록하지 않음 | 기록해도 됨 |
|---|---|
| API 키·서비스 계정 JSON·토큰 | 종목 코드·시장 데이터 |
| 계좌번호·잔고 실액 | 계좌 별칭 (`맘·쏘 삼성`) |
| 구글 시트 URL을 **새로** 추가 | 전략 로직·파라미터 |

⚠️ **구글 시트 URL이 이미 하드코딩돼 있다** — `app.py:211`, `pages/5_내 소형주 퀀트 포트.py:15`.
코드는 서비스 계정으로 인증해 읽으므로 시트가 **제한 공유**면 URL이 노출돼도 열리지 않는다.
시트 "일반 액세스"가 **제한됨**임을 확인했다 (2026-08-12). 보유 종목은 노출되지 않는다.
이 설정을 "링크가 있는 모든 사용자"로 되돌리지 않는다.

## 구글 시트

`내 퀀트 포트폴리오` — 소형주 포트의 저장소다. 탭 5개를 코드가 이름으로 찾는다.

| 탭 | 1행 헤더 | 코드 동작 |
|---|---|---|
| `ddo` `sso` `mom` | `종목명` `종목코드` `매수단가` `수량` | 읽기 + **쓰기** |
| `Config` | `key` `value` | 읽기 + **쓰기** |
| `StockInfo` | `종목코드` `종목명` `시장구분` `액면가` `상장주식수` | 읽기만 — 사용자가 손으로 채우는 종목 마스터 |

1. **서비스 계정 권한은 "편집자"다. 뷰어로 낮추지 않는다.** 코드가 시트에 쓴다 — 낮추면 저장이 깨진다
2. **시트를 손으로 꾸미지 않는다.** 저장 시 `ws.clear()`로 탭을 통째로 지우고 다시 쓴다. 메모 열·서식은 저장 한 번에 사라진다 (`StockInfo`는 예외 — 읽기만 하므로 안전)
3. **헤더는 앱이 쓴다.** 위 4개 탭은 저장할 때 컬럼명을 1행에 직접 기록하므로 손으로 만들 필요가 없다. 손으로 만들어야 하는 건 읽기 전용인 `StockInfo`뿐이다
4. 헤더가 어긋나거나 탭이 없어도 **에러가 안 난다.** `except: pass`로 삼키고 빈 값으로 넘어간다. 시트 관련 버그는 헤더부터 눈으로 본다
5. **종목코드는 `utils/data_loader.normalize_stock_code()`로 정규화한다. 사본을 만들지 않는다.** 우선주·전환우선주는 코드에 알파벳이 섞인다(`00104K` `37550K` `37550L`). `int(float(코드))`를 태우면 ValueError가 나고 `except: pass`에 삼켜져 포트가 통째로 빈 화면이 된다. `str.extract(r'(\d+)')`로 숫자만 뽑으면 터지지도 않고 **조용히 다른 종목**이 된다. 둘 다 실제로 났던 버그다 (2026-08-12 · 08-14 수정 완료). **파일마다 사본을 뒀기 때문에 같은 버그가 두 번 났다** — `app.py`·`pages/5`는 이 함수를 import만 한다. 상세는 `현황.md` §5번 · §6번

## DGbrain과의 관계

`C:\Users\DGgram\orca\projects\DGbrain` — 개인 기록소. 이 레포는 그쪽에서 **작업장**으로 지정돼 있다.

| DGbrain으로 | 여기 남김 |
|---|---|
| 백테스트 결과·결론 | 코드·파라미터·실행 절차 |
| 전략을 왜 채택/폐기했나 | 할 일·진행 상태 |

**쓰기는 DGbrain 폴더에서 연 세션이 한다.** 여기서 DGbrain에 직접 쓰지 않는다.
DGbrain `CLAUDE.md`는 이 세션에서 자동 로드되지 않고 활발히 개정 중이다. 인용 전에 다시 읽는다.
관련 위키 페이지: `wiki/quant/` — `my-quantum-jump-대시보드`, `백테스트-기록`, 전략별 5장.

## 톤

- 아첨하지 않는다. 근거가 부실하면 부실하다고 말한다
- 숫자를 다룰 때 출처와 가정을 명시한다. 미확인 가정은 그렇게 표시한다
- 백테스트 결과를 낙관적으로 해석하지 않는다. 과최적화 가능성을 먼저 본다
- 사용자는 git 경험이 없다. **git 작업은 단계를 설명하면서 진행한다**
